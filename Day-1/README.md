## 🧠 Day 1 – Introduction to SQL & SELECT Statement

---

### 📘 Topics Covered

* Basic `SELECT`
* Column selection
* Viewing data structure

---

### 📖 Overview

**SQL (Structured Query Language)** — also known as **SEQUEL** — is the standard language used to manage and query relational databases.

The `SELECT` statement is the **foundation of SQL**, and it is your primary tool for retrieving data from database tables.

---

### 🔑 Key Concepts

* Tables store data in **rows (records)** and **columns (fields)**
* `SELECT` defines **which columns** to retrieve
* `FROM` specifies **which table** to query
* SQL statements typically end with a **semicolon (`;`)**

---

### 🧾 Basic Syntax

```sql
SELECT column_name1, column_name2
FROM table_name;
```

```sql
SELECT *
FROM table_name;
```




